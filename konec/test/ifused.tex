\def\akarmi{valami_ize}
\def\valami{valami/ize}

`\lowercase{\ifused{VALAMI/ize}}{per after}{per before}'

`\ifused{\akarmi}[2]{dash after}{dash before}'

`\ifused{\valami}{per after}{per before}'

`\ifused{valami_ize}[2]{dash after}{dash before}'

`\ifused{valami/ize}{per after}{per before}'

`\ifused{valami_ize}[2]{dash after}{dash before}'

\newpage

`\ifused{valami/ize}{per after}{per before}'

`\ifused{valami_ize}[2]{dash after}{dash before}'

`\ifused{valami/ize}{per after}{per before}'

`\ifused{valami_ize}[2]{dash after}{dash before}'

`\ifused{valami/ize}{per after}{per before}'

`\ifused{valami_ize}[2]{dash after}{dash before}'
